# Sophia Chrome Plug-in

Sophia an oneline reading handler that plugin for Google Chrome.

## Installtion

npm install -g gulp bower yo

npm install -g generator-chrome-extension

yo chrome-extension

gulp babel

gulp watch

gulp build
